import React from "react";

const Error = () => {
    return (
        <section>
            <p id='errorMessage'>It looks like this url is invalid</p>
            <p>Please click an option in the menu bar to go to a valid url</p>
        </section>
    )
}

export default Error